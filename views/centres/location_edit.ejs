<% if(loggedIn) { %>
<div class="location-editor">
<div class="form-group">
    <form action=" <%= `/centre/locate/edit?id=${centres._id}&_m=put` %>" method="POST">
        <h2>Update Centre Location:</h2>
        <p>Name: <%= centres.name %> </p>
        <p>Former Address: <%= centres.address %></p>
        <p>You can fill in a new address and check its position by clicking Check</p>
        <p><label for="address">New Address</label>
        <textarea name="address" id="address" cols="70" rows="5" placeholder="house number, street, city, state" class="form-control"></textarea></p>
        <button id="check" type="button">Check</button>
        <p>Alternatively, you can use coordinates by clicking the button to use your current location or manually selecting the correct location on the map</p>
        <p><strong>NB:</strong> Do <strong>not</strong> fill in a new address if you are using coordinates.</p>
        <p><label for="location">Coordinates</label>
        <input type="text" name="location" id="location"></p>
        <button id="coordinates" type="button">Use current location</button>
        <button type="submit" class="btn btn-primary">Update</button>
        <button type="button" class="btn btn-danger" onclick="window.history.back()">Cancel</button>
    </form>
</div>
</div>
<div id="locatemap">
</div>
<div><input type="hidden" id="val" value="<%= centre %>" disabled></div>
<script
      src="<%= `https://maps.googleapis.com/maps/api/js?key=${apikey}&callback=locateMap&libraries=&v=weekly&region=NG` %>"
      async
></script>
<% } else { %>
    <p>You need to be logged in. Register/Log in <a href="/users/login">here</a> or go <a href="/">home.</a></p>
<% } %>